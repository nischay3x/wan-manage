#!/bin/bash

if [ $1 = "remove" ]; then

    if [ -e /var/run/fleximanage.pid ]; then
        PID=`cat /var/run/fleximanage.pid`
        if ps -p $PID > /dev/null
        then
          sudo systemctl stop fleximanage.service
	  rm -rf /lib/systemd/system/fleximanage.service
        fi
    fi

fi
